# My-Wallet
A Python CLI app and api endpoints where users can: add and view transactions, set spending budgets per category, track progress toward savings goals, and generate a monthly summary report — all saved to a file so no data is lost between sessions.
